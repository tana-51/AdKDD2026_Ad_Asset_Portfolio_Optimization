[project]
name = "AdKDD_AdPOM"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "clip",
    "hydra-core>=1.3.2",
    "obp>=0.4.1",
    "torch>=2.9.0",
    "wandb>=0.22.3",
]

[tool.uv.sources]
clip = { git = "https://github.com/openai/CLIP.git" }
